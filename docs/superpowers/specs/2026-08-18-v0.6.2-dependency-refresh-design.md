# GoBFD v0.6.2 Dependency Refresh Design

**Status:** Revised proposal after final-recommendations audit; implementation
not started

**Release type:** maintenance and security patch

**Compatibility:** GoBFD `bfd.v1`, YAML, and GoBGP v3 wire contracts remain unchanged

## Goal

Release `v0.6.2` with current supported packages, toolchains, CI actions, and
interop peers while retaining GoBGP `v3.37.0`, the final v3 release. The release
also corrects public claims that the independent RFC review disproved. It does
not add new BFD behavior.

## Baseline and target

The baseline is `origin/master` at `20eaaacd87f7a27ae446674ffec11dbce8177dc4`.
The latest published GoBFD release is `v0.6.1`.

| Class | Baseline | Target |
|---|---|---|
| Go | `1.26.3` | `1.26.6` |
| GoBGP | `github.com/osrg/gobgp/v3 v3.37.0` | unchanged |
| FRR interop | `10.2.5` mutable tags | `10.7.0` pinned by digest |
| BIRD interop | floating Debian `bird3` package | BIRD `v3.3.2` reproducibly pinned |
| GoBGP interop images | mostly `v3.33.0` | `v3.37.0` pinned by digest where available |

FRR `10.7.0`, BIRD `v3.3.2`, and GoBGP `v4.8.0` were the current upstream
releases on 2026-08-18. GoBGP v4 belongs to the separate v1 design.

## Go module closure

The dependency update uses the closure selected by `go get -u ./...`, followed
by explicit tool pins and `go mod tidy`.

| Module | Current | Target |
|---|---:|---:|
| `buf.build/gen/go/bufbuild/protovalidate/protocolbuffers/go` | `v1.36.11-20260415201107-50325440f8f2.1` | `v1.36.12-20260709200747-435963d16310.1` |
| `connectrpc.com/connect` | `v1.19.2` | `v1.20.0` |
| `connectrpc.com/grpchealth` | `v1.4.0` | `v1.5.0` |
| `google.golang.org/protobuf` | `v1.36.11` | `v1.36.12` |
| `google.golang.org/grpc` | `v1.81.0` | `v1.83.0` |
| `github.com/knadh/koanf/parsers/yaml` | `v1.1.0` | `v1.1.1` |
| `github.com/knadh/koanf/v2` | `v2.3.4` | `v2.3.6` |
| `github.com/prometheus/client_golang` | `v1.23.2` | `v1.24.1` |
| `github.com/reeflective/console` | `v0.1.25` | `v0.5.0` |
| `golang.org/x/net` | `v0.53.0` | `v0.58.0` |
| `golang.org/x/sync` | `v0.20.0` | `v0.22.0` |
| `golang.org/x/sys` | `v0.43.0` | `v0.47.0` |

All other direct runtime modules remain at their current versions unless the
resolved closure requires a transitive change.

Direct imports of archived `gopkg.in/yaml.v3` move to
`go.yaml.in/yaml/v3 v3.0.5`. The change covers the HAProxy agent, CLI formatter,
and integration tests.

## Required compatibility edits

The deprecated `golang.org/x/net/http2/h2c` wrapper is replaced by Go 1.26
`http.Protocols` with HTTP/1 and unencrypted HTTP/2 enabled. A plaintext
ConnectRPC/gRPC smoke test must prove that the server contract is unchanged.

The Go version changes to `1.26.6` in `go.mod`, CI, release jobs, and all Go
builder images. The Go 1.24 echo-reflector reference image remains unchanged.

## Tool and CI pins

| Tool | Target |
|---|---:|
| golangci-lint | `v2.12.2` |
| gopls | `v0.23.0` |
| govulncheck | `v1.7.0` |
| OSV Scanner | `v2.5.1` |
| Buf | `v1.72.0` |
| benchstat | `v0.0.0-20260813145340-fd4a688df892` |
| gotestsum | `v1.13.0` |

The dev image contains no `@latest` installs. `scripts/vuln-audit.go` uses the
Go 1.26 typed `errors.AsType[*exec.ExitError]` API. Tool dependencies move to a
separate `tools/go.mod`; the Go `tool` directive alone does not isolate tool
modules from the main module's Minimal Version Selection graph. Root Make and
CI wrappers invoke the pinned tools through that tools module.

CI action updates form a separate change set from runtime modules:

| Action | Current | Target |
|---|---:|---:|
| `actions/checkout` | `v6.0.2` | `v7.0.1` |
| `actions/setup-go` | `v6.4.0` | `v7.0.0` |
| `actions/cache` | `v5.0.5` | `v6.1.0` |
| `actions/setup-node` | `v6.4.0` | `v7.0.0` |
| `SonarSource/sonarqube-scan-action` | `v8.0.0` | `v8.2.1` |
| `bufbuild/buf-action` | `v1.4.0` | `v1.5.0` |
| `codecov/codecov-action` | `v6.0.0` | `v7.0.0` |
| `github/codeql-action` | `v4.35.3` | `v4.37.7` |
| Docker setup QEMU/buildx | `v4.0.0` | `v4.2.0` |
| `docker/login-action` | `v4.1.0` | `v4.6.0` |
| `goreleaser/goreleaser-action` | `v7.2.1` | `v7.2.3` |
| `ossf/scorecard-action` | `v2.4.3` | `v2.4.4` |
| gosec | `v2.26.1` | `v2.28.0` |

Every action is pinned to an immutable commit SHA in workflow files; the table
records the release from which that SHA is selected.

## Vulnerability policy

The Go vulnerability database reports `GO-2026-4736` against GoBGP v3 and v4
without a fixed release. The existing exception expired on 2026-07-31, so the
release remains red until maintainers make a separate recorded security
decision. If approved, that decision may permit exactly this advisory through
2026-11-30; it may not be silently renewed or broadened. Every other finding
fails the build.

The mitigation is routing-plane specific: explicit trusted BGP peers,
control-plane ACLs or GTSM where applicable, and gobgpd monitoring/restart.
Securing the management gRPC endpoint remains required but does not mitigate a
NEXT_HOP parser denial of service on TCP/179.

## Documentation truth correction

`v0.6.2` immediately corrects claims that are not supported by the current
implementation. This is documentation repair, not protocol expansion.

- RFC 5880 becomes “asynchronous core, partial”. Known blockers include
  outgoing Poll/timer semantics, remote Demand handling, diagnostic ordering
  and errata, and acknowledged AdminDown; the list is not treated as exhaustive
  until every stable gate in the v1 contract passes.
- RFC 5883 documents the current one-transit-hop TTL restriction instead of
  attributing `TTL >= 254` to the RFC.
- RFC 9384 is “not implemented; blocked by GoBGP API” because the wire subcode
  is Administrative Shutdown `/2`, not BFD Down `/10`.
- RFC 9468, RFC 9747, RFC 7130, RFC 8971, and RFC 9521 are marked preview or
  experimental with their exact limitations.
- RFC 9764 is not called complete for authenticated padded packets until the
  combined mode passes.
- The 16M packets/s figure is labelled a microbenchmark of unmarshal, demux,
  and channel delivery, not end-to-end UDP processing or a scale guarantee.
- The 10,000-session production narrative is removed. No supported scale is
  claimed from discriminator lookup benchmarks alone.
- The current all-interface plaintext management and metrics listeners and the
  unauthenticated flight-recorder endpoint are documented as unsafe defaults.
  Until v1 changes the runtime contract, deployment guidance must bind them to
  loopback or an isolated management namespace and keep debug exposure off.

English is the source; existing Russian counterparts receive the same factual
changes before release.

## Change-set boundaries

The release is split into independently reviewable local commits:

1. Go toolchain and runtime module closure.
2. YAML module path and native HTTP/2 compatibility edits.
3. Isolated developer-tool module and vulnerability policy.
4. CI action pins.
5. Reproducible GoBGP v3.37, FRR 10.7.0, and BIRD 3.3.2 interop peers.
6. EN/RU security, dependency, changelog, and RFC-claim corrections.

No GoBGP v4 import, public API change, BFD feature, or broad refactor belongs in
this release.

## Acceptance gates

```bash
go mod tidy -diff
go build ./...
go test ./... -race -count=1
go vet ./...
go tool golangci-lint run ./...
buf lint
buf breaking --against '.git#branch=master'
go run ./scripts/vuln-audit.go
make interop
make interop-bgp
make interop-rfc
make benchmark
make lint-docs
```

The release is accepted only when:

- all 19 Go packages are discovered and tested under Go 1.26.6;
- plaintext HTTP/2 ConnectRPC/gRPC works through native `http.Protocols`;
- GoBGP `DisablePeer` and `EnablePeer` remain compatible with v3.37.0;
- interop logs assert FRR 10.7.0, BIRD 3.3.2, and GoBGP v3.37.0;
- the controlled audit invokes `govulncheck`, records its expected non-zero
  result, and passes only when the sole finding is `GO-2026-4736` covered by
  the unexpired exact exception;
- hot-path benchmark allocation counts do not regress;
- EN/RU documents have matching headings and factual status;
- all required GitHub checks are green.

## Primary references

- [Go 1.26.6 release notes](https://go.dev/doc/devel/release#go1.26.6)
- [GoBGP v3.37.0](https://github.com/osrg/gobgp/releases/tag/v3.37.0)
- [GO-2026-4736](https://pkg.go.dev/vuln/GO-2026-4736)
- [FRR 10.7.0](https://github.com/FRRouting/frr/releases/tag/frr-10.7.0)
- [BIRD v3.3.2](https://github.com/CZ-NIC/bird/tree/v3.3.2)
